Ext.define('Leap.view.SponsorsDetailView', {
    extend: 'Leap.view.BaseView',
    alias: 'widget.sponsorsdetailview',

    constructor: function (config) {
        this.callParent(config);

        this.setTitle('Sponsors Detail');
        this.add([
 
        ]);
    }
});